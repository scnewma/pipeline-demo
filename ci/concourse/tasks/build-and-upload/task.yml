---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: springcloud/pipeline-base

inputs:
  - pipeline-demo

outputs:
  - name: out

caches:
  - path: maven

run:
  path: /bin/bash
  args:
    - tools/concourse/tasks/build-and-upload/task.sh